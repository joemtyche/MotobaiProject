ACTIVE TODO / IMPORTANT RISKS

ORDER + STOCK INTEGRITY
- Manually confirm the patched stock transition behavior:
  - Delivery: unvalidated -> validated should deduct stock once.
  - Delivery: validated -> shipped -> received -> completed should not deduct again.
  - Walk-in: unvalidated -> completed should deduct stock once.
  - Cancelled / returned should only return stock if stock was previously deducted.
- Watch edited order details on unvalidated orders.
  - Saving order-detail edits still calls order.order_tracking.save().
  - Current stock transition logic should prevent extra stock mutation, but this should be manually verified.

ORDER DETAILS / RETURN FLOW
- Fix or temporarily remove Return Order in the order details modal.
  - returnItems is declared but no longer populated by the current edit flow.
  - Return Order updates order details only; it does not reliably move the order to returned status.
  - Frontend references date_returned, but the backend OrderTracking model has no date_returned field.
- Verify order tracking update uses the OrderTracking id, not the Order id.
  - Current frontend endpoint uses /api/ordertracking/update/${orderId}/.
  - Backend route expects the OrderTracking primary key.

REFERENCE NUMBERS
- Move generated Delivery / Walk-in order reference numbers to the backend, or add backend retry handling.
  - Current frontend generation depends on the currently fetched order list.
  - Stale order data or two quick creates can create duplicate references.
- Keep Stock-in reference number manually editable.
- Keep Payment Reference # behavior separate from sales/order reference numbers.

FRONTEND FORM + TABLE CLEANUP
- Fix DynamicForm submit behavior.
  - Pressing Enter in a form can call the parent handler with a browser event instead of the form object.
  - The submit button currently works through a manual wrapper, but the form onSubmit is unsafe.
- Clean up DynamicForm floating labels so text boxes do not jiggle or overlap.
- Keep temporary product-list tables unsortable unless rows get stable local ids.
  - DynamicTable falls back to row index when an item has no backend id.
  - Sorting temporary rows by index can make row-click edits target the wrong item.
- Replace page reloads after create/update/delete/status actions with refetch or local state updates.
- Order Management and Order History now support combined status + order-type sidebar filters.
- Order History now shows Sales Reference # and Bill Reference #.

LINT / PRODUCTION CLEANUP
- Fix the real lint/runtime issues first.
  - WalkIn.jsx references an undefined callback after submit.
- Remove dev console.log/debug output from frontend and backend.
- Replace browser alert() session-expired messages with the app's modal/notification style.
- Decide whether to add prop-types or disable react/prop-types consistently.
- Fix Tailwind config warnings.
  - boxShadow values currently include trailing semicolons.
  - tailwind.config.js is an ES module but uses require().

DEPENDENCIES / CONFIG
- Run npm audit fix carefully and rebuild.
  - Current audit reports axios/form-data/react-router-related vulnerabilities.
- Keep this branch as local React + Django/MySQL.
  - Do not add backendless mode, frontend-only mode, or online backend deployment support here.
- Use backend/.venv or Python 3.11/3.12 for backend work.
  - Global Python 3.14 is not the intended backend setup.
- Register main backend models in Django admin for easier local data inspection.

CHECK STATUS
- Frontend build passes with warnings.
- Django manage.py check passes through backend/.venv.
- makemigrations --check --dry-run reports no model changes.
- Backend test suite currently has 0 tests.
